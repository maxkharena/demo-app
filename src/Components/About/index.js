// Absolute import
import React from 'react';

// Layout
import Layout from '../Layout';

const About = () => {
    return (
        <div>
           <h1>About</h1>
        </div>
    );
}

export default Layout()(About);